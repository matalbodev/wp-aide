---
import Index from "../components/Index";
import Layout from "../layouts/Layout.astro";
import { getHomeContent, getOffers } from "../utils/api";
const contentString = await getHomeContent();
const offersData = await getOffers();
---

<Layout title="homepage">
	<Index client:load contentString={contentString} />
</Layout>
